# E8.1 Gamma6 最后一轮关键窗救援

最终状态：**GAMMA6_NO_WITNESS_FOUND**。严格按用户授权完成一次诊断、一次定向候选扩展尝试和一次纯可行性搜索，之后停止。不是 INFEASIBLE，也不阻塞 Q4；Q4 已基于标准 Γ0 的 E7_001 完成。

## 诊断范围与结果

参考冻结 E7_001 的时间平移，最早发现的完整原子区间候选覆盖超载窗为 **752.875–829.328125 s**。这只是参考时刻表和完整区间候选关系下的最早覆盖超载，不是所有 L1 自由时刻及全部资源约束下的“最早不可行窗”证明。

活动运输任务为 M001–M006、M015、M016；早期硬截止闭包为 M001–M008（原始硬截止约束允许的最晚返航小于5000 s，5000 仅用于诊断分窗，不是新增硬截止）。完整 91 站点候选池的精确集合覆盖数是 **3**；窗口中点 791.101562500 s 由独立物理链路重算得到的瞬时覆盖数却是 **2**。

因此，整段保障要求与瞬时覆盖不能混淆，3 不能作为自由时序下“至少三架中继”的下界。这也说明不能仅从当前搜索失利就断言真实性能发生不连续下降。早期任务、硬截止、站点共享和往返/周转值得作为机制解释，但仍未证明哪项约束决定真实 Γ6 可行边界。

[诊断 JSON](e81/critical_window_diagnosis.json) 包括所有活动任务、允许平移上下界、共同站点交集、扫描过的事件窗口和集合覆盖见证。[占用表](e81/required_relay_occupation_intervals.csv) 逐任务—站点列出准备至返航周转完毕的必要占用区间。能源组件数量没有被证明是瓶颈，也没有被证明可以排除。

## 唯一扩展批次

关键硬截止任务细分为最多60 s的子段，整套保障任务由56段变为149段，早期部分121段；保障区间并集与原 Γ6 需求完全相同。

对关键窗选择8个缺少共同站点的任务对尝试定向扩展，每对最多250次检查。8对均被共同 LOS 必要几何域判空，未新增站点；没有补做第二批。对细分任务重新检查原完整候选池，得到 **91站点、3659条任务—站点关系**，每段有候选。不能将“定向扩展尝试”描述成“成功新增站点”。

## 唯一纯可行性搜索

零目标函数，不优化 J_late 或能耗。取消每架中继两/三次的限制，使用“每个关键原子任务最多锚定一个非空架次”这一有效分组数量上界，加9个窗口外固定中继组，共130个潜在架次。每个组以其最小原子编号为锚，避免仅因架次数设成2/3而漏解；没有设置任意每架架次数上限。运输时刻、实体机及同型电池采用 E7 L1 自由度。

模型包含 16358 个变量，其中 15989 个二元变量，48640 条约束。HiGHS 配置限时180 s，实测求解调用 193.807 s（包含求解器内部限时后的收尾），只调用一次。结果是 **TIME_LIMIT，无 incumbent**；没有可用于完整独立执行验证的见证。

有限候选、60 s区间、窗口外固定分组、连续活动悬停能耗保守约束仍限制搜索空间；能源组件在完整候选见证落地后核验，本次没有获得见证。搜索失败既不能证明有限模型不可行，更不能证明原问题物理不可行。

## 后续状态

E8-A：0/2/4 dB 鲁棒代价研究 DONE；ΓC_cert=4 dB。
E8-B：可行边界未定，最后救援停止，GAMMA6_NO_WITNESS_FOUND。
Q4：独立完成，不受 E8-B 门禁阻塞。
下一主线：全题提交表整合与论文主结果冻结；轻量 ns-3 为后期可选项。

诊断和止损记录经 [独立检查](e81/validation.json)：原需求并集保持、前序事件窗覆盖数不超过2、首个整段候选窗覆盖数3、中点独立物理覆盖数2，以及只执行一次救援搜索。旧 E8 原始结果未改写；本轮结果全部另存 e81。
