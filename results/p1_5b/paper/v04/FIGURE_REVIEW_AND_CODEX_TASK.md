# PAPER V0.4 图表审查与 Codex 素材任务

本文件只用于图表与数据生产，不承担论文正文撰写。

## 总体判断

现有 8 幅图的数据链完整，能够覆盖“总体框架—统一物理—Q1敏感性—Q2运输—Q3协调—近快端折中—资源日历—Q4分区”的主要证据。下一轮不增加图的数量，重点是删去开发语言、压缩过高图形、统一视觉语义，并保证赛事正文在单页/双栏转换后仍可读。

统一视觉语义建议：
- 运输：蓝色系；
- 中继/通信：橙色系；
- 环境/DEM/背景：灰色系；
- 可行/已满足：绿色系；
- 冲突或缺口：红色仅在必要处使用；
- 所有字体、线宽、图例位置和小数位统一。

## 图1 总体技术路线

保留。当前横向五阶段结构适合作为全文总览。

必须修改：
- 将“2903个自主模式”改为“2903个候选运输模式”；
- 不出现 Gate、版本号、内部方案编号；
- 最终阶段增加“独立验证”视觉反馈，但不要画成开发流程图。

主文作用：解释四问不是四个孤立模型，而是一条“运输→通信→分区”的数据继承链。

## 图2 统一物理模型

保留。建议仍采用“地形示意 + 载荷—能耗曲线”的双面板。

注意：
- 左图必须继续标注“示意图，不表示实测地形剖面”；
- 右图只标关键阈值和20%基准，不堆过多参数；
- 图题强调动态载荷与返航余量如何共同决定可行性。

## 图3 运输模式与资源排程

保留，但需要弱化“模式池规模展示”，强化“候选模式如何落到24个实际运输架次”。

建议：
- (a) 296个单点 + 2607个两点候选的简洁条形结构；
- (b) 主方案24架次—服务区访问关联；
- 机型A/B/C采用稳定颜色或符号；
- 不出现 F13 字样，正文统一叫“主方案”。

## 图4 运输—中继协调机制

重点保留，是方法章节的核心图。

建议：
- 上部机制链：三维轨迹 → 通信缺口 → 中继模式 → 占用冲突 → 必要反馈 → 时序重排；
- 下部只展示一个具有代表性的冲突/重排实例，不要铺满所有任务；
- 用虚线箭头表示反馈；
- 明确“求解超时 ≠ 不可行”。

## 图5 近快端折中

保留，当前尺寸和信息量合适。

必须修改：
- 点名统一为“极速端 / 中间方案 / 主方案 / 节能邻近方案”；
- 不显示 F12/F16/F13/F18；
- 横轴联合完工时间(min)，纵轴总能耗(kWh)；
- 每个点旁标运输+中继架次，如“24+3”；
- 主方案用不同标记，不使用“最优”字样；
- 不连成曲线或Pareto前沿。

## 图6 联合资源甘特图

当前 800pt 高度过大，不建议原图直接进入正文。

建议制作“正文紧凑版 + 附录完整版”两套：

正文紧凑版：
- 四个面板：运输无人机、运输电池、中继无人机、中继能源组件；
- 横轴统一使用 min；
- 每种资源只保留任务块，不标全部任务文字；
- 用竖虚线标出联合完工 104.097 min；
- 充电/周转延伸区与飞行区用不同填充方式；
- 高度控制在约 420–500 pt。

附录完整版：保留现有全部资源日历，用于核验。

## 图7 Q4 分区与资源缺口

保留，但建议进一步拆清“空间分区”和“资源代价”。

主文版：
- 左：K=2缺口优先空间分组；
- 中：K=3缺口优先空间分组；
- 右：四种代表的“总缺口—CV”散点或小表；
- 只对缺口优先代表标出分类型缺口；
- 不让地图线条与服务区文字重叠。

## 图8 Q1安全余量敏感性

保留并提前到问题一，论文编号应成为图3。

建议：
- x轴：返航剩余电量要求(%)；
- 主y轴：最少架次；
- 次面板：总能耗；
- 单独标出23.09%、27.05%两条临界线；
- 若仍展示S004/S008载荷能力，使用单独第三面板或简洁标注，避免双y轴混乱。

## Codex 仅执行的素材任务

1. 根据上述要求重绘图1、图3、图5、图6、图7、图8；图2和图4仅做文字与版面微调。
2. 每幅图同时导出 SVG、PDF、PNG 以及对应 CSV/JSON。
3. 图内不得出现：F12/F13/F16/F18、Gate、commit、autonomous、自主主例、external、challenge 等研发或外部竞赛表述。
4. 不修改任何模型、数值、方案或结论。
5. 不撰写论文正文。
6. 每幅图附一份 metadata：数据源、指标定义、可支持的结论、不能支持的结论。
7. 重新扫描所有图中文字，确保只使用官方数据和当前白名单结果。

建议输出目录：`results/paper_materials_02/`。
